# Test Fixtures

Committed sample inputs for tests belong under `tests/fixtures/`.

Current placeholder sequence:

- `tests/fixtures/placeholder-sequence/`

These placeholder images are intentionally simple and can be replaced later
with more realistic fixtures while keeping the same stable paths.
