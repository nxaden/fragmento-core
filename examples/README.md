# Examples

Committed sample inputs for manual experimentation belong under `examples/`.

Current placeholder sequence:

- `examples/media/placeholder-sequence/`

These placeholder images are intentionally simple. Replace them later with
real sample photography without moving the directory.
